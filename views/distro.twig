{% extends "layout.twig" %}

{% block content %}
<div class="container">
    <h1>{{ distro.name }}</h1>
    <a class="btn btn-default" href="{{ distro.web }}" target="_blank" role="button">Web</a>
    <hr>
    <p class="col-lg-12">{{ distro.description }}</p>
</div>
{% endblock %}